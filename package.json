{
  "name": "tset-subgraph1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ aitori/tset-subgraph1",
    "create-local": "graph create --node http://localhost:8020/ aitori/tset-subgraph1",
    "remove-local": "graph remove --node http://localhost:8020/ aitori/tset-subgraph1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aitori/tset-subgraph1"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.3",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
